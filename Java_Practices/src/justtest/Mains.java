package justtest;

import accessmodifier.Animal;
import accessmodifier.Mammals;

/**
 * Created by sgc on 2/18/16.
 */
public class Mains {
    public static void main(String []args){
      /*  System.out.println( max(2,3));
        Animal an = new Animal();
        an.size=6;
        System.out.println("size is:" +an.size);
        //an.colour="red"; can't be access outside the package show its shows error
        //an.volume=40.0; this can't be done since its outside package and volume is protected member
        Mammals mam = new Mammals();
        mam.size=4;
        mam.setColour("red");
        mam.getColour();*/

    }
   /* public static float max(float a,float b){
     if (a>b)
         return a;
        else
         return b;
    }*/
}
